<?xml version="1.0" encoding="UTF-8"?>
<sitemesh>
    <!-- Tạm thời không áp dụng decorator cho các trang hiện có -->
    <mapping path="/*" exclude="true"/>

    <!-- Loại trừ các API -->
    <mapping path="/api*" exclude="true"/>
</sitemesh>
